import heapq

class Solution:
    def kthLargest(self, arr, k):
        heap = arr[:k]
        heapq.heapify(heap)

        for i in range(k, len(arr)):
            if arr[i] > heap[0]:
                heapq.heapreplace(heap, arr[i])

        return heap[0]
        
        
        