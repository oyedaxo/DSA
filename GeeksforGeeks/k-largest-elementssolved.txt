import heapq

class Solution:
    def kLargest(self, arr, k):
        heap = arr[:k]
        heapq.heapify(heap)

        for i in range(k, len(arr)):
            if arr[i] > heap[0]:
                heapq.heapreplace(heap, arr[i])

        heap.sort(reverse=True)
        return heap