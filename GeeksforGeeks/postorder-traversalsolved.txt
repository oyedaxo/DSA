'''
class Node:
    def __init__(self, val):
        self.data = val
        self.left = None
        self.right = None
'''

class Solution:
    def postOrder(self, root):
        ans = []

        def dfs(node):
            if node is None:
                return

            dfs(node.left)
            dfs(node.right)
            ans.append(node.data)

        dfs(root)
        return ans
        