
    def uniqueOccurrences(self, arr):
        freq = {}

        for num in arr:
            if num in freq:
                freq[num] += 1
            else:
                freq[num] = 1

        seen = {}

        for count in freq.values():
            if count in seen:
                return False
            seen[count] = True

        return True