
class Solution:
    def findLucky(self, arr):
        freq = {}

        for num in arr:
            if num in freq:
                freq[num] += 1
            else:
                freq[num] = 1

        ans = -1

        for num in freq:
            if num == freq[num] and num > ans:
                ans = num

        return ans