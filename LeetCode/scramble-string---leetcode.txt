
                return False
                
            n = len(str1)
            for i in range(1, n):
                if check(str1[:i], str2[:i]) and check(str1[i:], str2[i:]):
                    memo[(str1, str2)] = True
                    return True
                    
                if check(str1[:i], str2[n-i:]) and check(str1[i:], str2[:n-i]):
                    memo[(str1, str2)] = True
                    return True
                    
            memo[(str1, str2)] = False
            return False
            
        return check(s1, s2)
